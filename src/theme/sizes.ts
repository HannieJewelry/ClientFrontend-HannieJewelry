// theme/sizes.ts

// --- Font Sizes (rem)
export const fontSizes = {
    xs: "0.6875rem",   // 11px: Caption rất nhỏ, tooltip, metadata
    sm: "0.75rem",     // 12px: Caption, label nhỏ, tag, thời gian, sub-label
    md: "0.875rem",    // 14px: Body2, secondary, form hint, nav, menu nhỏ
    lg: "1rem",        // 16px: Body1, primary, form label, button lớn
    xl: "1.125rem",    // 18px: Subtitle1, heading nhỏ, button lớn hơn
    "2xl": "1.25rem",  // 20px: Subtitle2, heading vừa
    "3xl": "1.5rem",   // 24px: H1, heading lớn
    "4xl": "1.875rem", // 30px: H2, heading lớn, headline
    "5xl": "2.25rem",  // 36px: H1, hero section
    "6xl": "3rem",     // 48px: Landing, số to, hero
};

// --- Icon Sizes (px)
export const iconSizes = {
    xs: 16,
    sm: 20,
    md: 24,   // Default MUI, Google
    lg: 32,
    xl: 40,
    "2xl": 48,
};

// --- Spacing (px)
export const spacing = {
    xs: 4,
    sm: 8,
    md: 12,
    lg: 16,
    xl: 20,
    "2xl": 24,
};
